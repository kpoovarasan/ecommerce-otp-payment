services:
  - type: web
    name: ecommerce-otp-payment
    runtime: python
    rootDir: ecommerce-otp-payment
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn --worker-class eventlet -w 1 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
